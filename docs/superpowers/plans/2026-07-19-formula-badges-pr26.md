# 配装数值、合规报告与称号记录实施计划

1. 为 `{0:10}` 倍率、昏厥 Lv1/Lv45 和最终面板编写失败测试，再实现通用格式倍率解析。
2. 为配装合规摘要编写后端与前端失败测试，复用现有自然/固定/未知/不可写校验器输出逐槽报告。
3. 导入并锁定 PR #26 的称号名称目录；先写目录、向量结构、门禁、单项/批量、并发和回读失败测试。
4. 实现称号后端事务，复用现有快照与原子写入，不合并 PR 的独立备份/回滚代码。
5. 以现有任务编辑页内部标签接入称号 UI，复用羊皮纸组件和确认弹窗，并测试筛选滚动、窄屏和中英文状态。
6. 用真实存档副本验证数值读取、武器阶段和称号向量；运行 Go、Node、构建与 Wails 原生多尺寸回归。
7. 更新版本适配、README 和最终完成度报告，清理过期 QA 口径，提交后非强制推送到用户仓库的当前功能分支。
