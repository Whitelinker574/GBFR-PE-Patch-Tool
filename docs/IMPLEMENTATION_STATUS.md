# Game 2.0.3 compatibility and implementation status

This page separates implemented behavior from open calibration work. “Implemented” means the code path and automated verification exist; “field-verified” additionally requires a repeated observation against the running game.

## v2.0.8 game 2.0.3 compatibility

The 2.0.3 archive comparison covers 2,120 `system/table` files and 332 reviewed character/combat MessagePack configs. Only four localization bundles changed; every project-used gameplay table and all reviewed configs are byte-identical to the locked 2.0.2 baseline. Static catalogs, loadout computation, sharing, Logs, and process-independent offline workflows therefore retain their verified data inputs.

The 2.0.3 executable is a different binary and all inspected runtime addresses moved. The shared high-privilege attach boundary hashes the executable before publishing the handle, module base, PID, or creation time, then selects the matching 2.0.2 or 2.0.3 layout. Each path still requires its own address, original-byte, recovery, and read-back checks; effect-level gaps remain experimental instead of being hidden behind a blanket legacy-version lock.

## v2.0.3 stable-promotion controls

The 2026-07-31 formal promotion is based on the current measured implementation, not a claim that every experiment has field-complete gameplay evidence. Runtime companion injection verifies the exact supported executable layout before ownership or DLL loading. Logs database BLOBs are length-checked and fetched through a bounded SQL substring before Go receives them. The release packager includes the third-party notice and all seven native license texts.

The stable build refuses new writes for cooldown tuning, shared three-character charge tuning, candidate runtime-patch catalog entries, candidate party-wide monster damage and virtual-sigil activation. Existing owned sessions can still be restored. These gates keep the UI and research evidence available without promoting unverified effects into supported stable capabilities.

## Implemented and covered

| Area | Current state |
| --- | --- |
| Multiple save slots | Save discovery is shared by offline editors and displays each detected slot independently. |
| Offline transaction safety | Backup, checksum repair, temporary output, atomic replacement and readback are implemented. |
| Sigil catalogs | Offline save generation, live-memory editing, loadout construction/import and virtual-sigil source creation share the same 2.0.3 catalog and synthesis-trait pool. Known shells keep their encoded primary and real secondary-slot structure; catalogued non-natural combinations and levels above the effect curve are writable with a warning. Unknown hashes, missing required fields, absent secondary slots and values outside the stored int32 range still fail closed. |
| Wrightstones | Offline save generation, live-memory editing and loadout import share the same trait catalog. Levels above the natural or effect-curve reference are writable with a warning; defaults remain natural values. Unknown hashes, empty required slots, duplicate traits and values outside the stored int32 range still fail closed. |
| Summons | Offline creation/update, live editing and loadout editing share catalog options. Type replacement creates a fresh SlotID, migrates equipped references and survives disk reopen; unpacked fixed-template levels are included. Natural rules remain advisory. |
| Loadout workspace | Weapons, twelve sigils, four active skills, mastery, permanent growth, Over Mastery and summons are editable. v3 single-loadout import creates twelve independent sigils, creates missing summons, and copies Master Level progress, permanent character enhancement, weapon progression, awakening/transcendence and the equipped wrightstone in one verified transaction. A missing matching weapon still blocks a partial write. |
| Pre-DLC saves | Editors remain usable when mastery/summon records are absent and do not claim that writing a record unlocks the DLC system. |
| Persistent patch pages | Combat, character and quest tabs share one owned connection; navigation does not unmount or silently restore enabled patches. |
| Automatic perfect guard | Two catalog sites plus the independent field override are guarded by the exact 2.0.2 executable identity and original bytes. The three-site form was re-verified during repeated combo hits, so the later table revision does not require stacking another overlapping patch. |
| Glass Cannon self-stun removal | The combat catalog contains one DLC 2.0.2 executable candidate at the unique guarded branch site. Enable changes only the original `75` branch byte to `EB`; disable and disconnect restore `75`, with write-back verification on both paths. This removes only Glass Cannon's own stun penalty and is not presented as immunity to other debuffs. The patch lifecycle and executable evidence are verified; the visible battle effect still needs a field sample. |
| Cooldown and shared-charge tuning | The application owns three 2.0.2 cooldown sites and one shared charge site through PID/creation-time leases, exact original-byte checks, near code caves, write-back verification and disconnect restoration. Cooldown exposes no-cooldown or a bounded multiplier, with “self” as the default and an explicit party candidate. Charge exposes instant or multiplier modes for the shared Io/Vaseraga/Ghandagoza path and conflicts with Ghandagoza's separate instant-punch patch. These are executable/lifecycle candidates: party coverage, role scope and visible timing effects still require gameplay samples. |
| Runtime monitoring | Solo party snapshots, selected-object monitoring and stable HP readback are implemented. Party equipment is now bound to the producing slot and online assignment; cross-slot/local-profile records fail closed instead of presenting the host's sigils as a teammate build. Three consecutive snapshots must also agree on the online character, weapon and sigil identity before a batch is considered stable. |
| Runtime sigil editing | Every write now requires the strict unified catalog path; merely observing an unknown runtime hash no longer makes it writable. The locked 2.0.2 `GEEN_109_24` row is cataloged as the official `怒发冲冠 V+` shell with fixed `怒发冲冠` primary and exact type-lot-15 secondary pool. Selecting a shell auto-restores its fixed primary and clears an incompatible secondary; the final live write, offline save write and loadout transaction repeat the same validation, so a stale UI or imported payload cannot reintroduce the invalid combination. |
| Natural Transmarvel sigils and item rewards | The experimental natural-drop page joins the locked 2.0.2 `gem.tbl` against the unified sigil catalog and patches shared gacha tables transactionally with `data.i` backup and restoration. Regular catalog items have a separate offline route through `reward.tbl`, `reward_lot.tbl` and `endlessmode_package.tbl`: the user can add bounded quantities and weights to Endless Mode “Forger's Bounty”, with a strict `1× / 2× / 4× / 8× / 16×` multiplier applied only to configured quantities. v2.0.8 adds an independent owned runtime switch at the audited task-result submission boundary: every type-1 ordinary stackable item already aggregated from a quest result is multiplied by `2× / 4× / 8× / 16×` and capped at 999. Shops, forging and manual inventory edits do not pass through this boundary; sigils, wrightstones, summons and weapons retain their normal instance count. On game 2.0.3 PID 53056, the hook installed at RVA `0x1FDA9C0`, read back as owned, then restored all 14 original bytes. Normal, boss, chest and side-objective result samples remain field acceptance rather than being inferred from that lifecycle check. |
| Online loadout sharing | Short-code publish/download uses a conservative one-shot transport and retries transient connection resets with a user-facing offline-code fallback message. Public catalog reads use a D1 search index with stable time/name/like cursors; publishing dual-writes the immutable R2 payload and D1 summary. A D1 outage falls back to a bounded complete R2 metadata scan that preserves the same global time/name/like ordering and cursor semantics, and fails closed above the 1,000-object proof limit instead of returning a misleading partial order. A token-protected bounded backfill migrates existing R2 metadata without exposing private save fields. Each detail page can also render a same-origin 1600×900 PNG, copy it through the browser image clipboard when supported, and otherwise download it locally. |
| UI/catalog icons | Character, item, weapon, sigil, summon and 261/262 playable-skill mappings use bundled official assets; `AB_PL2400_09` has no matching 2.0.2 PNG and uses the neutral fallback. |
| Loadout research workspace | The share workshop stays with each expanded preset, while “manual loadout / skill-target loadout” now switches directly in the factor header of the normal character editor. Skill-target mode sends the exact user-entered levels to the solver. All 29 playable characters now have at least one dated, direct-link Bilibili route transcribed from readable frames into exactly twelve physical sigil slots. Each reviewed graduation route exposes nine directly adjacent direction choices: unchanged graduation, high-HP and low-HP Celestial offense, defense, stun, sustain, potions/revive, dodge loop, and the two-piece Dread Black Pincer Crab set. Derived directions keep every character-specific core, replace only flexible physical slots, and show their per-trait swaps and conditional costs. The crab direction requires both set traits on the two audited `+` shells with fixed Crabvestment Returns instead of presenting one piece as an active set. Fixed-route assignment is linear and inventory-first, preserves distinct SlotIDs, and can require the exact Awakening/Celestial/crab shell and recorded fixed secondary instead of substituting an arbitrary same-primary shell. The video supplies only the visible sigil blueprint; derived directions are explicitly labelled as local 2.0.2 effect-based variants, and the catalog still gates legal shells, secondary traits and levels while unseen weapons, wrightstones, summons and mastery are never invented. Its owned-first domain searches real SlotIDs and legal catalog constructions together, preferring a complete inventory-only plan even when it uses more slots and creating independent sigils only for remaining gaps. Character-direction results and community routes remain dated model/player references, not field-proven global optima. Table-evidence, audited-catalog and current-inventory domains are also calculated separately; the table domain consumes only rows carrying explicit local 2.0.2 `gem.tbl` provenance. The inventory domain reads one versioned save snapshot and jointly ranks owned weapons, their bound wrightstones, four summons, saved mastery presets and twelve real sigil instances against a gearless baseline. The snapshot itself carries the selected preset's weapon, bound wrightstone, mastery and summons, so every caller gets correct replacement diffs without reconstructing state in the UI. Results include deterministic per-domain Top 10 rows, least-change, keep-current-equipment and robust-coverage alternatives, plus `[lo, hi]` coverage-rank sensitivity. Results preserve deployable SlotIDs and exact catalog hashes, expose unresolved effect atoms, and load the selected equipment and sigils into the normal reviewed editor transaction. Large state spaces are explicitly budgeted rather than labelled global optima. |
| Formula evidence kernel | The v2 TARDIS98 `MIN(C×D,2)` damage formula, optional OD attack/defense suppression, OD/berserk multipliers, multiplicative defense zones, 5.263% stable-cap margin and the distinct 0.27% / 0.07% / 0.02% summon probabilities are implemented as deterministic tested functions. The optimizer uses exact unpacked HP-curve nodes for Enmity, Stamina, Garrison and Stronghold and excludes unresolved intermediate `Smooth` / `SmoothSide` values instead of linearly inventing them. Character-specific move coefficients and uncaptured proc rates remain estimates or explicit omissions. |
| Sigil atlas handoff | A constructible 2.0.2 sigil can prefill the existing loadout constructor with its audited shell and primary/secondary traits. Optimization handoff resolves the applicable character, opens that character's loadout details directly in skill-target mode, and preserves the catalog's exact target levels. Save-writing remains a draft until the user selects a slot and performs the normal verified transaction. |
| Save laboratory | Two-save diff now attaches conservative bilingual field semantics, category/copyability/confidence filters and same-page left/right replacement for equal-shape records. Unknown fields remain explicitly unknown. Each write uses a revision check, backup, atomic replacement and per-field readback. Fate Episodes expose the audited 2.0.2 catalog and an experimental selectable writer for the 319 `3502` episode states and 56 `2561` mission states; only the known completion values are accepted. The writer proves file fields and recovery, not reward claiming or in-game effects. The 25-rule Endless atlas remains read-only because its effect values live in game tables rather than proven save fields, and no deployment/gameplay readback path exists yet. |
| Combat reference catalog | Unpacked 2.0.2 damage/guard configuration, 1,905 character damage-limit rows and seven conditional curves are available in loadout inspection. Raw nodes and exact `attackRate=1.0` table rows are shown without inventing unknown interpolation or final per-action caps. |
| Current-session damage capture | The integrated runtime can capture the 2.0.2 `ProcessDamageEvent` stream into a fixed 4,096-row shared-memory ring buffer. The app reports raw action IDs, exact pre-cap/cap comparison, overwritten-row counts and source-separated action summaries, and restores the owned Hook on explicit stop or app shutdown. Player/pet/summon ownership is not yet field-closed, so the UI labels totals as a global unattributed observation rather than personal DPS. |
| Game convenience runtime | One persistent integrated session owns damage-cap percentage display, decimal enemy HP/SBA display, room-ID capture with automatic clipboard copy and main-story Captain replacement. Native status binds the complete `{PID, creation time}` process identity, so a reused Windows PID cannot inherit an old active session. Normal-quest level sync and replaced-wrightstone return remain visible experimental options: their entry, post-write state, and restoration are verified, while quest-type coverage and full forge/inventory effects still require field checks. |
| Spatial diagnostics | Stable three-snapshot coordinate reading, restricted one-shot teleport, press-and-hold world-axis movement, in-game arrow-key control, and gravity suppression are implemented behind supported-version identity and topology checks. Held movement uses a user-entered 0.1–1000 units/second speed with an 8 units/second default and converts it to the bounded 45 ms write cadence. Each stable connection records its first player position as a session origin, and local named coordinate bookmarks only fill the teleport target instead of moving immediately. The gravity-write instruction at RVA `0x39DD964` keeps its separately owned restore path, exact-byte checks, and read-back; the stable UI exposes both activation and restoration. Scene combinations remain user-tested experimental coverage. Camera-relative movement and noclip are not implemented. |
| Monster patch recovery | HP, the previous partial-path damage entry, stun, overdrive and empty-overdrive hooks passed a live install/readback/restore integration run against the supported game process. The new shared health-delta damage entry passes 2.0.2 executable, install, readback and restore checks, but its claimed party-wide range and multiplier effect remain an explicit experimental candidate until multiplayer hit samples are captured. Every test releases its owned connection and restores the original bytes. |
| Integrated experimental runtime | Audio, town-camera and virtual-sigil hooks now live in the application-owned x64 `patch_core.dll` and use the existing `LoadLibraryW` injection path. Each enabled native companion receives the desktop tool's `{PID, creation time}` and holds a process watchdog; if the tool is force-killed, the DLL restores its owned hook/bytes and releases its owner file. A live 2.0.2 camera test killed the owning helper process, observed `inactive`, confirmed owner cleanup, then allowed a new instance to enable and restore the runtime. The shell queries one lightweight Go summary at startup and while visible, so active or recovery-required camera/audio/virtual-sigil sessions remain globally visible before their pages are opened and after page navigation. Audio scans the installed English/Japanese character banks plus the eight explicit `ui*.bnk` interface banks; role voices use `Volume_Voice`, UI effects use `Volume_SE`, and ambiguous/shared events fail open. Natural drops no longer require a loader or user-supplied table folder: the experimental app embeds the eleven exact 2.0.2 tables needed by summon, Transmarvel sigil, wrightstone and the verified Forger's Bounty item-package generation, bounds decompression, and validates every table by size/SHA-256 before use. Before any multi-file deployment write, it now persists a game-directory-bound prepare journal plus verified snapshots; a later process can roll a partial table/index/manifest transaction back without overwriting unknown third-party changes. The release build recompiles the native runtime and atomically publishes the verified DLL before embedding it. |

## Field evidence captured

- Io final HP was read repeatedly from the running game, including the stable `126,645` snapshot used to validate the monitor lifecycle.
- The Io EX node `1F52146F` is stored as float32 `0.4` but produces `+4` on the displayed stun scale; the versioned calibration record keeps the raw and displayed units separate.
- A repeated training-area comparison observed the same incoming attack with and without Defense +5%; the result is consistent with percentage damage reduction for that sample.
- Automatic perfect guard was retested after adding the second guarded site used during combo continuation.
- The monster HP, damage, stun, overdrive and one-shot empty-overdrive patches were installed and restored in one live 2.0.2 session; all original-byte and readback checks passed.
- The current-session damage runtime completed a live 2.0.2 start/status/stop lifecycle against PID 25660; the shared-memory protocol became active and the status returned to `inactive` with “hook restored after active callbacks drained”. No combat event attribution was claimed by this lifecycle-only check.
- Earlier convenience-runtime experiments completed six install/restore lifecycles, including level-sync and wrightstone-return hooks. Those runs prove lifecycle restoration, not every gameplay effect; the two mutating features remain available with an explicit experimental label and require effect-level acceptance checks.
- After stale `inactive` status handling was fixed, the current embedded runtime repeated session-only, free-Captain, display/session and all currently enabled features against the same live 2.0.2 process. All four combinations reached `active`, returned to `inactive`, released ownership and left the game running.
- The integrated audio and camera runtimes both completed a fresh 2.0.2 enable/disable lifecycle against PID 25660 and restored their owned hooks. The installed English/Japanese voice-bank validator passed 76,845 assertions; UI effects were independently discovered from the eight `ui*.bnk` banks and are not mixed into character voice ownership.
- The integrated virtual-sigil runtime completed a fresh enable/disable lifecycle against PID 25660 and returned its getter hook and native loop limits to `inactive`. Character switching, scene switching and same-character teammate isolation still require visible quest samples.
- The spatial page repeatedly read stable town coordinates, completed a no-displacement write/readback at the same coordinate, survived page navigation and restored its capture hook on disconnect.
- The previous camera runtime applied distance `9` and target height `2.6` in the live 2.0.2 camera object with write-back verification; an earlier `12`/`3.4` configuration and a subsequent hot update were also observed. Those observations must be repeated after the native-runtime migration.
- The game enumerated all eight configured virtual slots and served them from distinct real inventory instances (`SlotID 60103`, `60124`, `60126`, `47506`, `48314`, `54651`, `54735`, and `54736`) after the local status rebuild. The component loaded from its canonical directory without a stale replacement path or missing-DLL warning. The field configuration was restored to the four-slot default after the pressure test.
- The opt-in live release acceptance harness exercises camera, audio, QOL, damage capture and stable-player spatial writes in one supported 2.0.2 process, then verifies the stable gates reject new virtual-sigil and gravity sessions. A separate destructive helper-process test proves camera restores and releases ownership after the desktop owner is force-killed while the game remains running.
- On game 2.0.3 PID 53056, the migrated camera, audio, QOL, damage-capture, virtual-sigil entry, spatial/gravity, RuntimePatch and task-reward multiplier lifecycles completed install, status/read-back and restoration checks. The reward multiplier matched RVA `0x1FDA9C0` and restored its exact 14 original bytes. Normal-quest level sync, wrightstone return and actual reward-result quantities still require effect-level quest samples.

## Still open or only partially closed

| Request | Honest boundary |
| --- | --- |
| Complete SDK/game dump | Not performed and not required by the released application. Checked-in data comes from selected extracted tables, guarded executable sites, save layouts and field samples—not a claim of complete engine metadata. |
| Every character’s exact final formula | The common v2 damage and mitigation equations now participate in character-direction Top 10 ranking. The inventory domain varies owned equipment and sigils; catalog/table domains keep equipment fixed. Inventory mastery candidates are the presets actually saved for that character, not every mathematically legal mastery-node combination. Per-action coefficients, exact final caps outside recorded table nodes, character mechanics and several weapon/wrightstone/summon effect atoms are still incomplete, so the UI labels the result as a formula model rather than a proven global optimum. |
| Mastery unlock boundaries for every save state | Structural rank pools and save-backed caps are implemented. The full matrix of unopened, zero-MSP, low-rank and max-rank saves has not been field-sampled across multiple saves. |
| All permanent character growth | Extracted AP-tree/panel contributions are included where mapped. Irreversible growth and character-specific exceptions still require paired saves or cross-character evidence. |
| Defense and support interactions | The +5% sample is field-verified; the broader set of defense masteries, buffs, reductions and support interactions has not been generalized from that single sample. |
| Damage cap and conditional skills | Represented as separate sources/candidates, but battle/dummy samples are still required for exact effective caps and conditions. |
| Every runtime patch in every scene | Catalog guards and automated lifecycle tests exist for all published entries; only named critical paths have repeated live-game evidence. |
| Cooldown and charge effect coverage | The three cooldown signatures and shared charge signature are unique in the locked 2.0.2 executable, and automated tests prove install/readback/restore and fail-closed ownership behavior. They do not yet prove every cooldown producer, AI party member, character transition or the visible charge timing of all three named characters. |
| Personal/team attribution for current-session damage | Raw source instances are separated so identical action IDs from different sources no longer merge. Mapping pets, summons and remote players back to stable party identities still requires live quest samples; until then aggregate damage and DPS remain explicitly global and unattributed. |
| Unlocking absent DLC systems | The tool can edit encodable records but intentionally does not promise to set every progression flag needed to unlock systems the save has never opened. |
| Fate Episode write evidence | The experimental writer exposes only explicitly selected `3502` Fate states and `2561` mission-completion fields, bound to the inspected file revision. Real-save copies pass backup, atomic write, reopen, per-field readback, stale-revision rejection and forced-readback rollback tests. Secondary statistics/notification families such as `5801/5815`, claim states and dependency flags remain untouched and unproven, so the UI states that game effects and reward claiming are not verified. |
| Final action damage caps | Global baselines, character tables and raw conditional nodes are available. Action-specific producers and `Smooth`/`SmoothSide` interpolation are not proven, so the UI does not claim an exact final cap. |
| Endless task behavior | The 25 localized rules and five difficulty rows are exact unpacked references. They document task behavior but do not prove a runtime task hook or positive Conflux-timer activation inside a real Endless quest. |
| High-risk runtime experiments | Continuous world-axis movement, in-game arrow-key control, and independently restorable gravity suppression are implemented with process ownership, supported executable identity, bounded writes, and user-visible activation controls. They remain experimental across scene combinations and do not provide noclip or camera-relative movement. Natural-drop deployment validates all eleven exact source tables used by its summon, Transmarvel sigil, wrightstone and Forger's Bounty item-package outputs, preserves unrelated `data.i` fields, rejects conflicting external entries and uses a verified backup plus a crash-recovery journal on both supported executable identities. The separate task-result multiplier has a verified 2.0.3 install/readback/restore lifecycle, but ordinary, boss, chest and side-objective quantity outcomes still need quest samples. Wrightstones remain limited to the original four families, three variants per family and the legitimate 20/15/10 result shape. Audio, town-camera and virtual-sigil helpers share the integrated native runtime and fail closed. Audio covers uniquely attributable character-bank events and explicit UI banks while leaving shared/unknown events unchanged; English/Japanese bank parity and runtime restoration passed. Camera and virtual-sigil activation and explicit disable are available after migration; scene switching, teammate isolation and broader Hook coexistence remain user-tested experimental coverage. Virtual slots extend native runtime trait enumeration with 1-8 configured real inventory instances and never extend the save's 12 physical slots. Level sync and wrightstone return are available as experimental options with lifecycle restoration checks. |

The application must continue to label unresolved values as estimates, candidates, negative observations or open evidence. A green unit test proves a software contract, not a previously unobserved game formula.
