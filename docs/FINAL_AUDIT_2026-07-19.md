# GBFRDataTools 最终实现与完整性审计

审计日期：2026-07-19

工作分支：`feature/loadout-fullscreen`

工作树：`D:\gbf\.worktrees\loadout-fullscreen`
目标游戏版本：Granblue Fantasy: Relink 2.0.2

## 结论先行

本轮要求中的应用层、离线存档层、配装编辑器、界面统一、数据校验和运行时安全改造已经落地；相对 BitterG v1.8.4 的“新祝福生成崩溃修复”，本项目已在源码与自动测试层覆盖对应的寄存器保存、显式停止、原指令恢复和旧捕获清理，并增加了自动停 Hook、进程归属、整条事务写入、回读、回滚与不确定状态隔离。该崩溃场景仍需在完整游戏进程中实机复验。

当前结论是：

- **离线存档与应用 UI：实现已落地并通过本轮最终回归。** 因子、祝福、配装、专精、召唤石和单配装导入/导出都有对应测试路径；准确计数、哈希与截图矩阵见本报告验证表。
- **游戏最终人物面板已增加精确只读回读。** 新读取器从游戏自己的 2.0.2 状态对象取得 HP、攻击、昏厥和暴击；只有四处版本守卫通过且连续 3 份快照一致时才标记 `runtimeVerified`。句柄仅含进程查询与 `PROCESS_VM_READ`，不写内存、不注入。
- **离线草稿仍是显式 `≈` 估算。** Fate、Master 与基础四项已按真实存档/解包表修正；部分百分比 producer、条件效果及中间量化顺序尚未完全闭环，不能把草稿值冒充游戏最终值。
- **运行时功能仍需真实游戏进程验收。** 当前机器有独立 2.0.2 EXE 可做静态字节核对，但没有完整游戏资源或正在运行的进程，无法完成真实面板回读、内存编辑、铁匠铺切页和退出恢复 E2E。
- **并非所有图标和效果都有官方资源。** 项目现在只使用能精确联表的官方图，不再拿相似图冒充；缺失项会回退或显示未计入警告。

因此，“已知代码缺陷与本轮功能缺项”已经收束，但不能把“缺少游戏本体的真实进程验收”和“尚未逆完的游戏公式/资源”写成已经完成。

## 与 BitterG v1.8.4 的精确对照

公开发布记录显示：

- [v1.8.3](https://github.com/BitterG/GBFR-PE-Patch-Tool/releases/tag/v1.8.3) 新增“祝福生成-新”，可从背包选中、读取和修改；同时修复首页落在弃用补丁页及旧因子生成选择 DLC 主词条后缺副词条的问题。
- [v1.8.4](https://github.com/BitterG/GBFR-PE-Patch-Tool/releases/tag/v1.8.4) 只有一个发布说明：修复“祝福生成-新”导致游戏闪退。
- [v1.8.3...v1.8.4 对比](https://github.com/BitterG/GBFR-PE-Patch-Tool/compare/v1.8.3...v1.8.4) 是 1 个提交、4 个文件。
- [对应提交](https://github.com/BitterG/GBFR-PE-Patch-Tool/commit/0b0e36274bb8593735481341e0073ad8e5e26913) 的关键行为是保存/恢复 `R10`、增加显式关闭入口、恢复原指令并清空旧选中指针。发布说明写“祝福”，提交标题写“因子”，实际变更对象包含 Wrightstone 内存编辑。

| 上游版本 / 能力或修复点 | 本项目状态 | 本项目额外保护 |
|---|---|---|
| v1.8.3：背包选中祝福读取 | 已完成；自动测试通过，真机待验 | 页面预期地址、状态/代码洞捕获地址一致性复核，并绑定进程实例 |
| v1.8.3：三词条与等级编辑 | 已完成；自动测试通过，真机待验 | 整条记录编码，不做零散无回滚写入 |
| v1.8.4：保存/恢复 `R10` | 已完成代码保护；真机待验 | 自有代码洞标记、签名校验、拒绝收养未知 Hook |
| v1.8.4：显式关闭并恢复原指令 | 已完成代码保护；真机待验 | 页面停止、断开和应用退出统一进入恢复流程 |
| v1.8.4：清空旧选中指针 | 已完成代码保护；真机待验 | 写入成功后自动停 Hook 并清空捕获；停用失败保留恢复令牌供重试 |
| v1.8.4：防止铁匠铺其他操作崩溃 | 源码与自动测试覆盖；崩溃场景实机待验 | UI 明示风险；写入后立即自动停止，不依赖用户记得手动关闭 |
| 本项目：写入结果校验 | 超出上游 | 写入、游戏保存、内存回读、失败回滚、回滚不确定时隔离该进程 |

结论：**v1.8.4 没有额外遗漏的新页面或编辑能力；其源码修复点均有对应实现。** 仍未关闭的是同一铁匠铺崩溃路径的真实游戏进程复验，以及本项目更大的配装公式与资源边界。

## 本轮完成的主要工作

### 1. 配装编辑器重做

- 编辑配装改为进入独立大页面，正常配装列表仍保留右侧角色立绘；编辑页隐藏立绘，把空间留给配置和结果。
- 默认进入“构造因子”模式，不再默认从背包选择。
- 上方固定展示 12 个槽位、当前因子以及主/副词条；下方才是搜索、构造或背包候选，符合“先看配置，再替换”的操作顺序。
- 背包候选改为独立卡片布局，避免因子堆叠；构造模式以替换槽为核心，写入时才生成新因子并绑定到配装。
- 支持真实武器、四个可装备主动技能槽、完整角色技能候选、武器技能、四个召唤石、12 个因子和专精配置。
- 单个角色的一套配装支持 JSON 导入/导出；保留槽位顺序并报告缺失实例，不做用户已明确取消的批量导出。
- 写入采用计划、备份、写入、校验和回滚流程；构造出的新因子、技能、专精、召唤石与角色记录同步写入。

### 2. 专精规则与界面

- 专精放在因子配置下方，可完整展开，结果继续汇总到右侧。
- 展示三条专精方向及 R1、R2、R3、EX 阶段，不再只显示两个方向或缩略结果。
- R1 的三方向根技能可以并存；从 R2 起只能形成一个主方向，R3 必须沿用该方向。
- 没有选择主方向时，R2/R3 的普通子节点允许保留，但不会伪造对应方向的根效果。
- 等级配额、阶段依赖、方向冲突和导入数据都由前后端共同校验。

### 3. 属性结算与来源说明

- 头像下方显示 HP、攻击力、暴击率、昏厥值、伤害上限五项结果。
- 因子、武器技能、专精、角色上限突破、召唤石均进入汇总；武器技能不再遗漏。
- 同类加成先聚合，再按等级曲线封顶；同时显示投入等级、有效等级和溢出等级。
- 伤害上限保留普通、能力、奥义三类拆分，不把不同上限强行合成一个虚假值。
- 右侧提供来源账本，明确当前默认计算范围；未知召唤效果会显示哈希及“未计入”，不会静默消失。
- 新增游戏最终面板只读回读：从 RVA `0x7C24980` 的角色状态 manager 经 ID vector 与 hash map 找到目标角色，读取状态对象 `+0x04/+0x08/+0x10/+0x14` 的 HP、攻击、昏厥、暴击。
- 回读受 RVA `0xD4321`、`0x2DC081`、`0x2DC11E`、`0xA296F3` 四处 2.0.2 字节守卫保护，并要求连续 3 份快照完全一致才返回 `runtimeVerified=true`；句柄只有查询与 VM_READ 权限。
- 修正角色永久基础线：真实伊欧存档 `1309/1310/1312/1313` 分别给出 `3156 HP / 666 ATK / 8 Stun / 5 Crit`；`1318=0x7FF` 联 Fate 表得 `HP +640 / ATK +165`，`1323=3,309,499` 联 Master 表并钳制 Lv50 得 `HP +6000 / ATK +3000 / DmgCap +100`，所以无装备基础线为 `9796 HP / 3831 ATK / 8 Stun / 5 Crit`。
- 离线草稿的四项结果继续显示 `≈`；只有真实游戏回读结果显示精确值，避免 UI 混淆证据等级。
- 公式证据、RVA、字段、舍入点和真实回归值写入 [`FORMULAS_2.0.2.md`](FORMULAS_2.0.2.md)。

### 4. 因子、祝福与数据真值

- 修正 `GEEN_156_24`：本地 2.0.2 `gem.tbl` 证明其主词条为 `SKILL_023_00 / 0xCAC6AFF2`，不再误用 `SKILL_156_00`。
- 角色专属因子 91 项全部按本地 `gem.tbl` 与二词条 lot 联表：25 项固定副词条，66 项使用已验证的随机池；固定副词条不能省略或替换为任意词条。
- 无法证明自然生成合法性的条目不能进入构造队列；等级限制只公开自然范围。
- `7net / Seven Net` 已由 2.0.2 简中/英文/日文本地化表、`dlc.tbl` 与真实存档共同确认为 Seven Net Shopping 零售商特典，不是占位数据；目录已修正为物品/主词条等级 6。真实记录使用特殊 `flags=22`，普通构造器只写 `flags=2`，因此保留真实显示但禁止伪造构造。
- `0x40223C28` 按本地 `ids.txt` 和生成表修正为 `SKILL_143_00`，不再错误标成 `SKILL_166_50`。
- 因子/祝福离线生成器增加实例级串行化、队列副本返回、数量后端上限、写后回读和失败恢复；失败重试不会重复追加。

### 5. 运行时写入安全

- 因子、祝福、召唤石、上限突破、货币、药水、素材消耗、巴武掉落和怪物增强写入统一加入进程租约/所有权校验。
- 写入前后复核进程 PID、模块基址、捕获地址和旧值；页面令牌不能跨进程复用。
- 同一进程上的互斥运行时功能统一进入冲突门禁，避免两个页面同时改相同代码或状态。
- 所有关键写入增加回读；失败时尝试恢复原值并再次回读。无法证明回滚成功时会隔离当前进程实例，拒绝后续写入。
- 断开、页面停止、应用退出和进程重启走同一生命周期；恢复失败不丢弃令牌，可继续重试。
- 上限突破代码洞保存/恢复 `RFLAGS`、`R10`、`R11` 并保持栈平衡；每个槽位按 16 字节整块写入和回读，部分失败会回滚已写槽位。
- 伤害统计中的本进程裸指针访问改为系统内存读写 API，消除 `go vet` 的不安全指针告警。

### 6. UI 统一与自适应

- 恢复羊皮纸与场景背景，不再以不透明深色大面板覆盖整个工作区；纸张、金色描边、按钮、输入框、卡片和弹窗继续使用同一套设计令牌。
- 正常工具页保持“左侧功能面板 + 右侧大幅立绘”的双栏结构；立绘取消小容器框并随可用高度缩放。进入配装编辑时隐藏立绘，编辑器改为铺满工作区，不再以固定小面板居中。
- Q 版角色图与紧凑主页入口在窄窗口继续可达；标题栏右上固定最小化、最大化/还原、一键全屏、关闭四个窗口控制，关闭按钮不再漂到中间。
- 祝福石内存编辑的“启用读取后选中目标”改成紧凑行内提示，不再用居中大空框占据主内容。
- 字体、行高、换行、列宽、间距和卡片密度继续按容器断点收束；正常 960 宽仍保留双栏语义，编辑器在窄宽度按三列、两列、单列逐级折叠。
- Kimi 3、GLM 5.2 与 Fable 5（max 推理配置）已通过用户提供的模型渠道真实审阅当前 UI；三者共同指出的固定列溢出、立绘高度、标题状态宽度和编辑返回入口已纳入本轮实现与门禁。
- 多尺寸 QA 脚本直接控制实际 Wails WebView2，并用 CDP `Page.captureScreenshot` 截取应用内容；本轮对首页与 17 个工具页按 9 种窗口尺寸生成 162 张应用内截图，162/162 通过。
- 英文设置页不再承诺“完整翻译”；当前英文明确标成部分覆盖。

## 数据与图标完整性

| 类别 | 精确覆盖 | 处理原则 |
|---|---:|---|
| 角色头像 | 29/29 | 全部为官方紧凑头像 |
| 可玩主动技能 | 261/262 | 261 项按 2.0.2 `ability.tbl` 精确映射；唯一缺 `AB_PL2400_09` 的官方 PNG |
| 主动技能官方文件 | 243 个唯一 PNG | 与 `GBFR-UI-Reference-Library-2.0.2.zip` 逐字节一致；运行时不再引用旧社区图 |
| 词条图标 | 183/184 | `SKILL_023_00 / 0xCAC6AFF2` 已按 `skill.tbl` 的 `IconId=01_00` 精确映射到 `cmn_icskill_01_00.png`；唯一 `SKILL_112_00` 的官方 IconId 为空，使用明确回退 |
| 武器图标 | 159/163 | 缺 4 个 DLC 武器的精确资源；相似武器图未冒充 |
| 召唤石实体 | 189/189 | 背包/配装实体全部有精确图标 |
| 召唤主加护效果 | 170/230 | 170 项有权威效果；60 项显示哈希警告并明确不计入 |
| 召唤石副参数 | 22/22 | 可计算字段均有审计曲线 |
| 普通物品 | 301/312 | 11 个当前目录项在给定资源中没有精确 PNG |

本轮完整扫描了用户提供的 UI Reference Library、Portrait/Skill Portable、DLC 表和本地解包表。当前精确覆盖为主动技能 `261/262`、词条 `183/184`、武器 `159/163`、普通物品 `301/312`。缺失资源确实不在这些包中；项目选择显示默认回退/文字和明确缺口，而不是用“看起来像”的图替代。旧社区技能 PNG 仍作为未引用文件保留在工作树中，但运行时映射数量为 0。

## 最终验证记录

| 门禁 | 结果 |
|---|---|
| `runtime_character_panel` 容器、版本守卫、范围与三快照单元测试 | PASS：8 组顶层测试，含 9 个损坏容器/非法数值子用例，`0.902s` |
| Go 全量测试：`go test -mod=mod -count=1 -timeout=10m ./...` | PASS，`183.698s`；本轮命令同时启用隔离真实存档集成测试 |
| 前端 Node 全量测试：`node --test src/*.test.js` | PASS，157/157，0 fail/skip，TAP `1243.562ms` |
| Vite 生产构建：`npm run build` | PASS，Vite 3.2.11，108 个模块；仅保留主 JS chunk 大于 500 KiB 的非阻断警告 |
| `go vet ./...` | PASS |
| `git diff --check` | PASS |
| 真实存档读取/写入/回读 | PASS：隔离副本 2/2；另按明确授权执行原测试存档同配装原位写回，7 字段回读通过；写前/写后/备份 SHA-256 均为 `6F3F590787FEBE4ABF3B64C8AE7E6E9F202319959CE856E8334CC8B1C6B2688F` |
| Wails Windows 构建 | PASS，`14.338s`；EXE `60,549,120` bytes，SHA-256 `F4EBD115CE8C311BD00E6B00963008D348014960656662F1717FBFAAE8F556F7` |
| 注入模块构建 | PASS：以本机 MSVC `v143` 命令行覆盖项目缺失的 `v145` 工具集；DLL `38,400` bytes，SHA-256 `5686CFB8B695B02A184E27F0908D4533D29782114129F926C90DFA6571836B83`；保留 C4819/LNK4098 非阻断警告 |
| Wails 应用内全页面、多尺寸 WebView2 截图 | PASS，18 页面状态 × 9 尺寸 = 162/162；0 控制台错误、横向溢出、文字裁切或破损图标；最大化/全屏/还原实点通过 |
| Wails 配装真实存档交互脚本 | PASS，33/33；覆盖 12 因子、1269 背包卡、8 角色技能、武器技能、4 召唤石、四阶三方向专精、导入导出及固定基准 |
| Go race detector | 当前 Go 配置为 `CGO_ENABLED=0`，且没有 Go race 所需的 GCC/MinGW 工具链；未执行，不是通过项 |
| 真实游戏进程 E2E | 当前机器只有独立 2.0.2 EXE 供静态核对，没有完整游戏资源/运行进程；未执行，不得写成通过 |

常规集成测试只对隔离副本的二代临时副本写入，应用内截图/交互同样使用复制到隔离 `LOCALAPPDATA` 的真实存档。本轮另外按用户对测试存档的明确授权执行一次同配装原位写回；自动快照位于 `C:\Users\Admin\AppData\Roaming\gbfr-player-info-edit\save-backups\20260719-133045.653873100`，源文件在写入前后保持字节级相同。

## 还有没有没做完的

有，以下是不能用代码单方面伪造“完成”的边界，按优先级排列。

### P1：需要游戏环境才能关闭

1. **2.0.2 真实进程验收**：需要在目标 EXE 指纹对应的游戏中依次测试因子、祝福、召唤石、上限突破、货币/药水、素材/掉落、怪物增强的启用、写入、切页、停止、断开和退出恢复。
2. **新祝福铁匠铺崩溃场景复验**：需要实际执行“启用 → 选中 → 写入自动停用 → 切换武器强化/其他铁匠铺页”，确认与 v1.8.4 的真实崩溃场景完全闭环。

### P1：公式逆向仍未闭环

1. HP/攻击百分比效果在上游如何变成绝对贡献、不同乘区顺序及每次中间 `float32` 量化点尚未全部定位。
2. 2.0.2 的 ATK↑/DEF↓ 额外伤害、echo、supplemental 与各动作上限的最终顺序尚未闭环。
3. 精确只读路径可在游戏运行时直接取得四项最终面板，绕过离线公式缺口；但当前环境不能执行真实进程 E2E。离线草稿继续明确标记 `≈` 和未计入项，因此这是精度边界，不是静默给错值。

### P2：给定数据/资源本身的缺口

1. 1 个可玩主动技能、1 个词条、4 个武器、11 个物品缺精确官方图标；唯一缺词条 `SKILL_112_00` 在官方表中没有 IconId。
2. 60/230 个召唤主加护没有足够的本地权威效果定义；目前已逐项警告但未猜值。
3. 因子目录有 187 项，而本地 2.0.2 `gem.tbl` 可直接联表 184 项；另外 3 项不能仅凭名字推断自然生成规则。
4. `secondary-trait-rules.json` 仍保留少量低/中置信历史规则和 TODO；本轮已把 91 个角色专属因子改为本地表的 25 个固定池与 66 个随机池，其他无法证明的组合继续 fail-closed，不能宣称整个游戏的所有随机池都已穷尽。

### P2：兼容实验室

任务结算倒计时/零帧开箱、无限挑战、脸部/其他皮肤紫色符文、全称号、全队伤害统计、任务得分倍率、强制支线奖励和任务内倍率保留了旧资料或实验实现，但未完成 2.0.2 生产验收，UI 已明确隔离为兼容实验室。

### P3：非阻断工程项

1. 英文界面仍是部分覆盖。
2. 1269 个背包因子使用完整卡片列表；功能正确，但还可做虚拟滚动和大数据性能基线。
3. 前端主包仍有大 chunk 警告，可继续拆分异步页面和资源索引。
4. `go.mod` 使用 Wails 2.12.0，而本机构建 CLI 为 2.13.0；当前能构建，但发布环境应统一版本。
5. Windows race detector 需要安装 C 工具链并启用 CGO 后补跑。
6. 怪物增强 Hook 停止时会恢复并验证入口字节，但出于避免 DLL 卸载竞态的考虑，远程代码洞及已加载模块暂不在游戏存活期间执行 `VirtualFreeEx`/卸载；单次启用会留下很小的远程分配，游戏退出后由系统回收。

## 交付物

- 最终应用：`build\bin\GBFRDataTools-final.exe`，`60,549,120` bytes，SHA-256 `F4EBD115CE8C311BD00E6B00963008D348014960656662F1717FBFAAE8F556F7`
- 注入模块：`build\bin\patch_core.dll`，`38,400` bytes，SHA-256 `5686CFB8B695B02A184E27F0908D4533D29782114129F926C90DFA6571836B83`
- 本报告：`docs\FINAL_AUDIT_2026-07-19.md`
- 公式证据：`docs\FORMULAS_2.0.2.md`
- v1.8.4 设计与实施记录：`docs\superpowers\specs\2026-07-19-v184-live-editing-design.md`、`docs\superpowers\plans\2026-07-19-v184-live-editing.md`

最终发布判断：**离线存档、前端、Wails 应用内视觉/交互和构建门禁均已通过；代码范围内的已知问题已经收束。运行时功能在完成目标游戏 2.0.2 的真实进程验收前仍只能标为候选构建，不能写成“全场景已实机验证”。**
