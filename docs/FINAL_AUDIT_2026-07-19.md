# GBFRDataTools 最终实现与完整性审计

审计日期：2026-07-19

工作分支：`feature/loadout-fullscreen`

工作树：`D:\gbf\.worktrees\loadout-fullscreen`

目标游戏版本：Granblue Fantasy: Relink 2.0.2

## 结论先行

本轮已将离线存档、运行时注入、运行时监测和工具设置分成四个顶层分类。对本地 **CT 0.8.4** 的可枚举直接补丁候选，安全覆盖为 **60/64**：58 个进入新的生产目录，2 个复用项目已有的更安全实现；另 4 个因上游自述不安全或在锁定的 2.0.2 EXE 中签名多命中而明确拒绝。没有为凑数而暴露无法证明安全的开关。

当前可以做出以下结论：

- **CT 直接补丁中能安全落地的部分无已知遗漏。** 58 个新目录功能分为战斗 9、角色 37、任务/便利 12；连同 2 个已有实现，覆盖为 60/64。
- **其余 57 个叶脚本不能被粗暴记作 57 个“未集成功能”。** 它们是代码洞、Lua、参数或游戏内保存动作，其中部分已有结果等价实现，其余需要真实进程、寄存、场景和生命周期验证；未验证项不提供伪按钮。
- **CT 0.8.4 没有离线 `SaveData.dat` 编辑。** 项目的离线存档页是独立解析与写入实现；即使结果可等价，也不应记作“移植了 CT 的离线功能”。
- **游戏最终人物面板已有精确只读回读路径。** 读取器从 2.0.2 游戏状态对象获取 HP、攻击、昏厥和暴击；只有版本守卫通过且连续 3 份快照一致时才标记 `runtimeVerified`。
- **离线人物数值仍是显式 `≈` 估算。** Fate、Master 和基础四项已根据真实存档/解包表修正，但部分百分比 producer、条件效果和中间 `float32` 量化顺序尚未完全闭环，不会冒充游戏最终值。
- **最终生产资产已经入库并完成去重。** 20 个活动工具页映射的 20 张立绘与 20 张 Q 版素材均为 20/20 不同 SHA-256 和 20 个不同角色；未被生产映射的旧素材保留但不会打包。角色机制页已由重复的姬塔换为 Portable v3 新生成的巴萨拉卡立绘与 Q 版。最终测试、构建、真实存档回写与 21 页原生 WebView2 尺寸矩阵结果见“验证记录”。

因此，仍不能把“缺少完整游戏进程验收”或“尚未逆完的公式/资源”写成已经完成。

## 审计对象与分母

本报告同时涉及两个不同的上游对象，不得混用版本号：

1. **本地 Cheat Engine 表 CT 0.8.4**：`D:\gbf\GFR_v0.8.4_CHS_祝福石修改hotfix(Uno.ct`，494,532 bytes，SHA-256 `B75DF049E27D1423FC5ECDD47CC85DBAC241BEE582A49CEBA30CF020E150B659`。它是本报告“功能覆盖”的主要比对基准。
2. **BitterG GBFR-PE-Patch-Tool v1.8.4**：GitHub 发布版，本轮主要对照其新祝福/因子捕获崩溃修复。

静态唯一性核对锁定 `D:\gbf\granblue_fantasy_relink.exe`：123,522,016 bytes，SHA-256 `63340832BCF731FBC97796F686B05C988418E83D451D4A49B2244A85D00E297F`。这些是输入证据哈希，不是待生成应用的产物哈希。

## CT 0.8.4 功能清单与集成结果

### XML 结构口径

- CT XML 共 **370** 个 `CheatEntry`，其中 **152** 个带 `AssemblerScript`。
- 扣除 1 个根控制器和 2 个外部链接脚本后，有 **149** 个产品脚本。
- 按可操作叶子去除组标题、参数容器和说明后，有 **121** 个叶脚本控件；另有 **150** 个直接字段。
- 因此，370 不是“370 个独立功能”，152 也不是“152 个安全开关”。可比生产分母是 64 个直接补丁候选，其他 57 个叶脚本需按代码洞、Lua、参数或保存动作单独审查。

### 64 个直接补丁候选

| 结果 | 数量 | 说明 |
|---|---:|---|
| 新生产目录 | 58 | 战斗 9、角色 37、任务/便利 12；共 81 个补丁站点、79 个不同 AOB，在锁定 EXE 的可扫描 PE 段中均唯一命中 |
| 复用项目已有实现 | 2 | CT 31060“无限 Link Time”与 CT 31456“巴武/终末武器掉落”已有独立所有权和恢复流程，不重复搬运 CT 字节 |
| 明确拒绝 | 4 | 2 个上游自述不安全；2 个在锁定 2.0.2 EXE 中签名多命中 |
| **安全覆盖** | **60/64** | **93.75%**；不把拒绝项包装成生产功能 |

四个拒绝项是：

| CT ID | 功能 | 处置 | 证据 |
|---:|---|---|---|
| 31935 | 欧根瞬间蓄力/蓄力射击 | 拒绝 | CT 自述停用可导致崩溃，属已知不安全 |
| 33086 | 无限重复任务 | 拒绝 | CT 自述为实验/可能有问题，不进入生产页 |
| 31066 | 无视奥义槽限制 | 拒绝 | 第二签名 `NBGFR019B` 在锁定 EXE 中命中 2 处，无可证明的目标选择依据 |
| 31960 | 一击三幕/Threefold Shroud | 拒绝 | `NBGFR040` 在锁定 EXE 中命中 3 处，无唯一目标 |

CT 33487“强制获得支线目标奖励”已以 **3 个唯一站点**进入 quest 生产目录，不得再列入旧实验功能。

### 其余 57 个非直接补丁叶控件

| CT 功能族 | 数量 | 本项目对应状态 |
|---|---:|---|
| 玩家/队伍指针 | 5 | 队伍快照已做只读监测；货币值有既有路径；玩家名称未集成 |
| 战斗代码洞 | 12 | 已有安全等价实现的继续复用；需寄存/时序/场景实验的动作不暴露伪按钮 |
| 角色代码洞 | 19 | 直接字节项已分到角色生产页；完美连段、蓄力等需真实场景证明的代码洞仍门禁 |
| 仓库/选中记录/保存动作 | 15 | 因子、召唤石、祝福石实时编辑已有；选中素材/关键物品仅只读捕获；当前武器无等价实时编辑器，选中物品写入/调用游戏保存函数仍未通过生命周期验证 |
| 任务动作 | 2 | 任务得分、场景依赖动作仍需真实任务验收 |
| 便利/Lua/参数动作 | 4 | 动作速度、自动支线、免费制作等需现场上下文或可逆性证明的动作仍门禁 |
| **合计** | **57** | 57 是残余叶控件数，不等于 57 个已证明安全的独立可移植功能 |

尚未通过真实进程事务验收的“全队回血”、“触发 Link”等即使技术上看似可做，也不提供可点击的生产按钮。角色/成就外链、不可逆 Steam 成就等跨系统动作同样不自动集成。

### CT 与离线存档的边界

CT 0.8.4 **没有离线 `SaveData.dat` 解析或文件写入**。它的“仓库/保存”项通过 `openProcess`、NBLib、代码洞和游戏内保存调用作用于正在运行的进程。本项目的离线因子、祝福、配装、专精、召唤石等页面是独立的存档格式实现；即使可以产生部分结果等价的数据，也不应记作“移植了 CT 的离线存档功能”。

## 与 BitterG PE Patch Tool v1.8.4 的精确对照

公开发布记录显示：

- [v1.8.3](https://github.com/BitterG/GBFR-PE-Patch-Tool/releases/tag/v1.8.3) 新增“祝福生成-新”，可从背包选中、读取和修改；同时修复首页落在弃用补丁页及旧因子生成选择 DLC 主词条后缺副词条的问题。
- [v1.8.4](https://github.com/BitterG/GBFR-PE-Patch-Tool/releases/tag/v1.8.4) 只有一个发布说明：修复“祝福生成-新”导致游戏闪退。
- [v1.8.3...v1.8.4 对比](https://github.com/BitterG/GBFR-PE-Patch-Tool/compare/v1.8.3...v1.8.4) 是 1 个提交、4 个文件。
- [对应提交](https://github.com/BitterG/GBFR-PE-Patch-Tool/commit/0b0e36274bb8593735481341e0073ad8e5e26913) 的关键行为是保存/恢复 `R10`、增加显式关闭入口、恢复原指令并清空旧选中指针。发布说明写“祝福”，提交标题写“因子”，实际变更对象包含 Wrightstone 内存编辑。

| 上游能力或修复点 | 本项目状态 | 本项目额外保护 |
|---|---|---|
| 背包选中祝福读取 | 代码与自动测试路径已有；真机待验 | 页面预期地址、状态/代码洞捕获地址一致性复核，并绑定进程实例 |
| 三词条与等级编辑 | 代码与自动测试路径已有；真机待验 | 整条记录编码，不做零散无回滚写入 |
| 保存/恢复 `R10` | 已实现对应保护；真机待验 | 自有代码洞标记、签名校验、拒绝收养未知 Hook |
| 显式关闭并恢复原指令 | 已实现对应保护；真机待验 | 页面停止、断开和应用退出统一进入恢复流程 |
| 清空旧选中指针 | 已实现对应保护；真机待验 | 写入成功后自动停 Hook 并清空捕获；停用失败保留恢复令牌供重试 |
| 防止铁匠铺其他操作崩溃 | 源码与自动测试覆盖；真实崩溃场景待验 | UI 明示风险；写入后立即自动停止，不依赖用户手动关闭 |
| 写入结果校验 | 超出上游 | 写入、内存回读、失败回滚，回滚不确定时隔离该进程实例 |

结论：**就 GitHub v1.8.4 相对 v1.8.3 的单次修复增量而言，没有额外遗漏的新页面或编辑能力；其源码修复点均有对应保护。** 这个结论不代表“CT 0.8.4 所有代码洞/Lua 动作都已移植”。

## 应用页面分类与数量

当前导航源码共有 **20 个工具页 + 1 个首页 = 21 个顶层页面状态**。各组已按常用优先排序：

| 顶层分类 | 页数 | 页面 ID |
|---|---:|---|
| 存档修改（离线） | 6 | `loadoutPresets`、`sigil`、`progression`、`wrightstone`、`chara`、`save` |
| 内存注入（实时） | 10 | `runtime`、`sigilMemory`、`wrightstoneMemory`、`loadout`、`summon`、`overlimit`、`ctCombat`、`ctCharacters`、`ctQuest`、`monster` |
| 内存监测（只读结果） | 1 | `ctMonitor` |
| 工具与设置 | 3 | `compatibility`、`language`、`patch` |
| 首页 | 1 | `home` |
| **合计** | **21** | 顶层页面状态 |

配装编辑全屏态是 `loadoutPresets` 内部工作区，监测页的“队伍/选中物品”是页内标签，都不重复计为新的顶层页。旧的实验性实时顶层页已从生产导航和渲染分支删除；旧的“首页 + 17 工具页 = 18 页”和 `18 × 9 = 162` 截图口径均已过期。

## 本轮主要实现

### 1. 配装编辑器

- 编辑配装改为进入独立大页面；正常配装列表仍保留右侧角色立绘，编辑态隐藏立绘以腾出空间。
- 默认进入“构造因子”模式；上方固定展示 12 个槽位、当前因子和主/副词条，下方才是搜索、构造或背包候选。
- 背包候选使用独立卡片，构造模式以替换槽为核心；写入时才生成新因子并绑定配装。
- 支持真实武器、武器技能、4 个可装备主动技能槽、4 个召唤石、12 个因子和完整专精配置。
- 单个角色的一套配装支持 JSON 导入/导出；保留槽位顺序并报告缺失实例，不做用户已取消的批量导出。
- 写入采用计划、备份、写入、校验和回滚流程；新因子、技能、专精、召唤石与角色记录同步写入。

### 2. 专精规则与界面

- 专精放在因子配置下方并可完整展开，结果汇总到右侧。
- 展示三条专精方向及 R1、R2、R3、EX 阶段。
- R1 的三方向根技能可以并存；从 R2 起只能形成一个主方向，R3 必须沿用该方向。
- 没有选择主方向时，R2/R3 普通子节点允许保留，但不会伪造对应方向的根效果。
- 等级配额、阶段依赖、方向冲突和导入数据由前后端共同校验。

### 3. 属性结算与真实面板

- 头像下方显示 HP、攻击力、暴击率、昏厥值和伤害上限五项结果。
- 因子、武器技能、专精、角色上限突破和召唤石进入汇总；同类加成先聚合，再按等级曲线封顶。
- 伤害上限保留普通、能力、奥义三类拆分，不强行合并成一个虚假值。
- 右侧来源账本明确默认计算范围；未知召唤效果显示哈希和“未计入”，不静默消失。
- 精确只读路径从 RVA `0x7C24980` 的角色状态 manager 经 ID vector 与 hash map 定位目标角色，读取状态对象 `+0x04/+0x08/+0x10/+0x14` 的 HP、攻击、昏厥、暴击。
- 回读受 RVA `0xD4321`、`0x2DC081`、`0x2DC11E`、`0xA296F3` 四处 2.0.2 字节守卫保护，并要求连续 3 份快照完全一致才返回 `runtimeVerified=true`；该路径句柄只有查询与 `PROCESS_VM_READ` 权限。
- 真实伊欧存档 `1309/1310/1312/1313` 分别给出 `3156 HP / 666 ATK / 8 Stun / 5 Crit`；`1318=0x7FF` 联 Fate 表得 `HP +640 / ATK +165`，`1323=3,309,499` 联 Master 表并钳制 Lv50 得 `HP +6000 / ATK +3000 / DmgCap +100`，无装备基础线为 `9796 HP / 3831 ATK / 8 Stun / 5 Crit`。
- 离线四项继续显示 `≈`；只有真实游戏回读结果显示精确值。公式证据、RVA、字段、舍入点和真实回归值见 [`FORMULAS_2.0.2.md`](FORMULAS_2.0.2.md)。

### 4. 因子、祝福和数据真值

- 即时祝福编辑页已与离线祝福存档编辑统一为暖色羊皮纸视觉，不再使用深色面板；三条词条均使用统一的 `CatalogSelect` 搜索选择器，并保留当前等级、哈希和写入状态的紧凑展示。
- 即时祝福三槽在宽屏下并列；容器宽度不超过 760px 时第三槽跨满整行，避免 2+1 排列留下半屏空洞。连接状态、捕获状态与写入操作均随页面正常滚动，写入区不再悬浮遮挡内容。
- `GEEN_156_24` 按本地 2.0.2 `gem.tbl` 修正为主词条 `SKILL_023_00 / 0xCAC6AFF2`，不再误用 `SKILL_156_00`。
- 91 个角色专属因子按本地 `gem.tbl` 和二词条 lot 联表：25 项固定副词条，66 项使用已验证随机池；固定副词条不能省略或替换为任意词条。
- 无法证明自然生成合法性的条目不进入构造队列；等级限制只公开可证明的自然范围。
- `7net / Seven Net` 由 2.0.2 中/英/日本地化表、`dlc.tbl` 与真实存档共同确认为 Seven Net Shopping 零售商特典，不是占位数据。真实记录使用特殊 `flags=22`，普通构造器只写 `flags=2`，因此保留真实显示但禁止伪造构造。
- `0x40223C28` 按本地 `ids.txt` 和生成表修正为 `SKILL_143_00`，不再误标为 `SKILL_166_50`。
- 因子/祝福离线生成器有实例级串行化、队列副本返回、后端数量上限、写后回读和失败恢复；失败重试不重复追加。

### 5. 运行时安全和监测边界

- 因子、祝福、召唤石、上限突破、货币、药水、素材消耗、巴武掉落和怪物增强写入统一加入进程租约/所有权校验。
- 因子、祝福石和上限突破的旧 Wails 兼容写入口也统一进入同一租约门禁：只要新页面持有 owner token，旧 `Enable/Update/Set/Disable` 调用会在发现或连接进程前直接拒绝，避免旧接口绕过页面所有权；三组完整变更入口均有回归测试。
- 写入前后复核 PID、进程创建时间、模块基址、捕获地址和旧值；页面令牌不能跨进程复用。
- 同一进程上的互斥运行时功能进入冲突门禁；关键写入进行回读，失败时回滚，无法证明回滚成功时隔离当前进程实例。
- CT 生产目录的 81/81 个站点都携带从锁定 2.0.2 EXE 提取的原始字节证据；建立租约前必须逐字节命中 `expectedOriginalBytes`，不一致就拒绝写入。75 个含通配掩码的站点（含 2 个全通配补丁目标）也不能再绕过原字节门禁。
- 代码洞安装使用“从未发布 / 已安装 / 发布尝试后已恢复 / 需恢复”状态。只有能证明从未发布的代码洞才立即释放；可能被任意线程看到过的入口即使已恢复，也把代码洞退役保留到进程断开，避免执行中线程 UAF。
- 多字节代码写入没有暂停目标进程全部线程；回读、失败回滚和租约隔离可以证明事务结果，但不能把它表述为 CPU 指令抓取层面的原子写入。该边界仍需真实游戏进程场景 E2E 验收。
- 队伍监测仅使用只读路径，不安装 Hook。
- 选中素材/关键物品监测会临时安装两个只用于捕获选中地址的 Hook，并在停止/断开时恢复入口。它不修改数量、标志或存档，但不得被描述为“完全零内存写入”。

### 6. UI、自适应与立绘

- 首页保留场景插画并完整占满可用高度；普通工具页只使用无角色、边缘带花纹的羊皮纸背景。纸张、金色描边、按钮、输入框、卡片和弹窗继续复用统一设计令牌，目录恢复旧版左上花纹并用独立纸张层次与主内容区分。
- 正常工具页保持“左侧约 62% 功能面板 + 右侧大幅立绘”双栏视觉结构；立绘是独立于网格的固定右侧背景层，不放进小盒子，也不随窗口缩放漂移，只有配装编辑工作区隐藏立绘。
- 标题栏右上只保留最小化、最大化/还原、关闭三个窗口控制，不再提供多余的全屏按钮；窄窗口中独立首页入口和 Q 版形象仍可达。
- 顶部工具标签使用粗体字重；存档、实时注入和工具组按常用功能优先排列，首页同步补齐祝福石即时编辑和配装录制/复刻入口。
- 版本适配页已按最新生产导航重算为 20 个工具页 + 1 个首页，并分列 6 个离线存档页、10 个实时注入页、1 个只读监测页、3 个工具/设置页。页面同时展示 60/64 安全覆盖、58 功能/81 站点/79 AOB 目录证据、逐类图标覆盖，以及“真实游戏进程 E2E 待实机验证”的明确边界。
- 用户可见页面标题、页签、首页入口和兼容性基线中不再附带 CT 版本后缀；本报告仍保留 **CT 0.8.4** 作为输入证据和审计分母，避免把界面命名简化误写成来源变化。
- 祝福石内存编辑的“启用读取后选中目标”使用紧凑行内提示，不再以居中大空框占据主内容。
- 新增的 CT 战斗、角色、任务和监测页按功能分别映射立绘与 Q 版，生成依据 `D:\gbf\GBFR-Relink-Portrait-Skill-Portable-20260718-v3.zip` 的分阶段工作流。角色机制页重新经过页面 brief、官方身份锚点、线稿、4K 完稿、2K Q 版、透明去底和边缘安全校验，最终使用巴萨拉卡与真实武器“巨镰古洛诺斯”；不再与角色使用次数页的姬塔重复。
- 20 个活动工具页各自保留固定背景立绘的独立光学校准；因子晶体、召唤晶体、任务印章/书、版本罗盘、运行时钱袋和角色机制三槽互斥台等页面语义道具都在真实 Wails 截图中可见。20 张生产立绘与 20 张生产 Q 版的文件哈希和角色身份均 20/20 唯一。
- Kimi 3、GLM 5.2 与 Fable 5 参与本轮页面评审；Fable 5 请求显式携带 max 推理请求头。三者的意见只作为设计输入，最终是否通过仍以真实 Wails WebView2 的尺寸矩阵、控件可达性和无裁切/重叠证据为门禁；未经原生截图复核的推测不直接落地。

## 数据与图标完整性

| 类别 | 精确覆盖 | 处理原则 |
|---|---:|---|
| 角色头像 | 29/29 | 全部为官方紧凑角色图标 |
| 可玩主动技能 | 261/262 | 261 项按 2.0.2 `ability.tbl` 精确映射；唯一缺 `AB_PL2400_09` 对应官方 PNG |
| 主动技能官方文件 | 243 个唯一 PNG | 映射文件与 `GBFR-UI-Reference-Library-2.0.2.zip` 官方资产一致；运行时不引用旧社区技能图 |
| 词条图标 | 183/184 | `SKILL_023_00 / 0xCAC6AFF2` 按 `skill.tbl` 的 `IconId=01_00` 映射到 `cmn_icskill_01_00.png`；唯一 `SKILL_112_00` 官方 IconId 为空，使用明确回退 |
| 武器图标 | 159/163 | 缺 4 个 DLC 武器的可证明精确资源，不以相似武器图冒充 |
| 召唤石实体 | 189/189 | 背包/配装实体全部有精确图标 |
| 召唤主加护效果 | 170/230 | 170 项有权威效果；60 项显示哈希警告并明确不计入 |
| 召唤石副参数 | 22/22 | 可计算字段均有审计曲线 |
| 普通物品 | 301/312 | 11 个当前目录项在给定资源中没有可证明精确 PNG |

图标目录由 UI Reference Library、Portable 资产、DLC 表和本地 2.0.2 解包表联合生成。缺失资源不在已给定的可证明资产集中；项目选择显示中性回退/文字和明确缺口，不使用“看起来像”的图片替代。旧社区技能 PNG 可以作为未引用文件保留，但生产映射数量必须为 0。

## 验证记录

### 已固定的中间证据

| 证据 | 状态 |
|---|---|
| CT 输入文件大小与 SHA-256 | PASS：494,532 bytes；`B75DF049E27D1423FC5ECDD47CC85DBAC241BEE582A49CEBA30CF020E150B659` |
| 锁定 2.0.2 EXE 大小与 SHA-256 | PASS：123,522,016 bytes；`63340832BCF731FBC97796F686B05C988418E83D451D4A49B2244A85D00E297F` |
| CT 生产目录结构 | 源码测试固定 58 功能 / 81 站点 / 79 不同 AOB，并检查排除项、nibble mask、原字节、回滚字节和冲突组 |
| CT 原字节证据 | PASS：目录 schema v2 的 81/81 个站点均有 `expectedOriginalBytes`；证据生成器锁定 CT SHA、EXE SHA/大小、站点 ID、偏移、长度和 RVA，无证据或已启用镜像直接拒绝生成 |
| 直接补丁静态唯一性 | 生产目录 79 个不同 AOB 在锁定 EXE 的可执行段中均唯一，81 个站点的 RVA 与原字节逐项复现；CT 31066 为 2 命中，CT 31960 为 3 命中，已拒绝 |
| CT 目录/真实 EXE/代码洞安全焦点回归 | PASS：设置 `GBFR_GAME_EXE_TEST=D:\gbf\granblue_fantasy_relink.exe` 后执行 `go test -mod=mod -count=1 -run 'TestCT084ProductionCatalog\|TestCT084CatalogMatchesLocalGame202\|TestInstallCodeHookAtomic\|TestRuntimeHookInstallFailure\|TestReleaseCT084SelectedCapture' ./...`，`2.556s` |
| 代码洞发布失败安全 | 焦点测试已覆盖从未发布、恢复后退役、无法证明恢复时保留租约/隔离写入等状态 |
| 真实存档最终回归 | PASS：隔离副本与明确授权的原测试存档都已在最终后端源码上回归；最新原位写入备份位于 `C:\Users\Admin\AppData\Roaming\gbfr-player-info-edit\save-backups\20260719-150516.026803000` |

### 最终发布门禁

| 门禁 | 最终状态 |
|---|---|
| Go 全量测试：`go test -mod=mod -count=1 -timeout=20m ./...` | PASS：设置真实 CT、锁定 EXE 和真实存档副本环境变量后，主包 `597.190s`，`tools/ct084_originals` `0.160s` |
| Go 静态检查：`go vet ./...` | PASS：最终源码 `7.051s` |
| 前端 Node 全量测试：显式枚举 `src/*.test.js` 后执行 `node --test` | PASS：219/219，0 fail，`3.429s` |
| Vite 生产构建：`npm run build` | PASS：118 modules；主 CSS `211.76 KiB` / gzip `31.34 KiB`，主 JS `1303.66 KiB` / gzip `375.94 KiB`。仅保留大于 500 KiB 的非阻断拆包建议 |
| `git diff --check` | PASS：最终报告和二进制入库前后均无空白错误 |
| 真实存档读取/写入/回读 | PASS：明确启用 `GBFR_REAL_SAVE_WRITE_QA=1` 后对 `D:\gbf\Saved\SaveGames\SaveData2.dat` 执行原位写入、自动备份和回读；测试 `8.971s`，写前/写后/备份 SHA-256 均为 `6F3F590787FEBE4ABF3B64C8AE7E6E9F202319959CE856E8334CC8B1C6B2688F` |
| Wails 应用内页面与多尺寸 WebView2 截图 | PASS（最终源码矩阵）：直接启动包含最终 `appVersion` 的 QA Wails EXE，通过其应用内 WebView2 CDP `Page.captureScreenshot` 采集 21 页 × 5 档（960×620、1024×768、1440×900、1920×1080、2560×1392）= 105 张；105/105 layout check、0 console error。输出目录仅有 105 张新截图，无旧实验页残留；桌面和紧凑尺寸均复核即时祝福三槽、版本适配、角色头脸、Q 版、语义道具、目录花纹、主页高度和控件可达性，并以断言确认可见工具版本精确为 `v1.9.1-local-dlc202`、不含 CT 后缀 |
| 配装真实存档交互脚本 | PASS（代码级）：全量 Go 回归以真实存档读取 12 因子（主/副词条）、4 主动技能、武器/武器技能、50 专精、4 召唤石，并完成单角色单配装 JSON 导出→导入→编辑上下文往返；页面布局另由 Wails 原生矩阵验证 |
| Wails Windows 清理构建 | PASS：最终源码执行 `wails build -clean -platform windows/amd64 -o GBFRDataTools-final.exe`，`43.977s`；产物 62,641,664 bytes，SHA-256 `1F6F24181F01A8E96342044C785E2C590538E03EA293DFA6A016B1E8FAC97DAC`；隐藏启动 3 秒冒烟通过且正常关闭。CLI 2.13 / go.mod 2.12 版本提示保留为 P3 |
| 注入模块构建 | PASS：MSBuild Release x64 / v143，0 error、2 warning；`resources\patch_core.dll` 为 38,400 bytes，SHA-256 `72928633D9E59CA2723CF1341688A9112E9022C27D3ED1F5ED2FAF32D356EF5F`，由 `app.go` 的 `go:embed` 嵌入 EXE。警告为 C4819 源文件编码和 LNK4098 默认库冲突 |
| Go race detector | 未执行/环境门禁：当前 `CGO_ENABLED=0` 且 `gcc` 不存在；Windows race 构建条件不满足，不记作 PASS |
| 真实游戏进程 E2E | 未执行/环境门禁：本轮没有连接正在运行的 2.0.2 游戏进程；静态 EXE 唯一性与模拟内存测试不能替代实机场景 |

## 还有没有做完的

有。以下是不能用代码或文案伪造“已完成”的边界。

### 已完成的最终产物门禁

1. 最新立绘、Q 版和图标入库后，Go、Node、Vite、`go vet`、真实存档和 `git diff --check` 已重跑。
2. 21 页 × 5 档实际 Wails 窗口矩阵已在用户可见 `appVersion` 最后一处文案调整后完整重跑；即时祝福、版本适配、羊皮纸、双栏比例、立绘/语义道具、窄屏首页、文字和控件可达性均为 105/105、0 console error。
3. DLL 与正式 EXE 均已按最终源码重新清理构建并记录大小、SHA-256；没有沿用早期构建数字。

### P1：真实游戏进程才能关闭

1. **2.0.2 真实进程验收**：依次测试因子、祝福、召唤石、上限突破、货币/药水、素材/掉落、怪物增强和 58 个 CT 目录补丁的启用、冲突、停止、断开和退出恢复。
2. **新祝福铁匠铺崩溃复验**：实际执行“启用 → 选中 → 写入自动停用 → 切换武器强化/其他铁匠铺页”。
3. **CT 监测页**：验证队伍快照的实际容器布局，以及选中素材/关键物品 Hook 安装、捕获、恢复、页面退出与进程断开。
4. **57 个非直接叶控件的进一步筛选**：完美连段/蓄力、任务得分、动作速度、自动支线、免费制作、选中物品写入和当前武器保存等在无真实事务证据前继续门禁。

### P1：公式逆向仍未闭环

1. HP/攻击百分比效果如何变成绝对贡献、不同乘区顺序及每次中间 `float32` 量化点尚未全部定位。
2. 2.0.2 的 ATK↑/DEF↓、echo、supplemental 与各动作上限的最终顺序尚未闭环。
3. 精确只读路径可在游戏运行时直接取得四项最终面板，但当前环境不能执行真实进程 E2E。离线草稿继续标记 `≈` 和未计入项，这是精度边界，不是静默给错值。

### P2：给定数据/资源缺口

1. 1 个可玩主动技能、1 个词条、4 个武器、11 个普通物品缺可证明的精确官方图标；唯一缺词条 `SKILL_112_00` 在官方表中没有 IconId。
2. 60/230 个召唤主加护没有足够的本地权威效果定义；当前逐项警告但不猜值。
3. 因子目录有 187 项，本地 2.0.2 `gem.tbl` 可直接联表 184 项；另外 3 项不能仅凭名字推断自然生成规则。
4. `secondary-trait-rules.json` 仍保留少量低/中置信历史规则和 TODO。本轮已把 91 个角色专属因子改为本地表的 25 个固定池与 66 个随机池，其他无法证明的组合继续 fail-closed。

### P2：未进入稳定界面的旧实验资料

旧实验顶层页已经从生产导航、组件分支和稳定运行时页面中删除。任务结算倒计时/零帧开箱、无限挑战、脸部/其他皮肤紫色符文、全称号、全队伤害统计和任务得分倍率只可能保留未引用资源、旧资料或未验收底层实现，不会在稳定界面提供伪按钮。**CT 33487“强制获得支线目标奖励”不在此列；它已是三站点生产目录功能。**

### P3：非阻断工程项

1. 英文界面仍是部分覆盖。
2. 1269 个背包因子使用完整卡片列表；功能正确，但还可做虚拟滚动和大数据性能基线。
3. 前端主包的大 chunk 警告可通过继续拆分异步页面和资源索引改善。
4. `go.mod` 与本机 Wails CLI 版本应在正式发布环境统一。
5. Windows race detector 需要安装 C 工具链并启用 CGO 后补跑。
6. 已经可能被发布的远程代码洞在入口恢复后会退役保留到进程断开，以避免执行中线程 UAF；这会在游戏存活期间保留少量远程分配，进程退出后由系统回收，是明确的安全取舍。

## 交付物

- 最终应用：`build\bin\GBFRDataTools-final.exe`

  62,641,664 bytes；SHA-256 `1F6F24181F01A8E96342044C785E2C590538E03EA293DFA6A016B1E8FAC97DAC`。
- 注入模块源产物：`resources\patch_core.dll`

  38,400 bytes；SHA-256 `72928633D9E59CA2723CF1341688A9112E9022C27D3ED1F5ED2FAF32D356EF5F`；通过 `go:embed` 进入最终 EXE。
- 本报告：`docs\FINAL_AUDIT_2026-07-19.md`
- 公式证据：`docs\FORMULAS_2.0.2.md`
- CT 0.8.4 设计与实施记录：`docs\superpowers\specs\2026-07-19-ct084-runtime-integration-design.md`、`docs\superpowers\plans\2026-07-19-ct084-runtime-integration.md`
- v1.8.4 祝福/因子安全设计与实施记录：`docs\superpowers\specs\2026-07-19-v184-live-editing-design.md`、`docs\superpowers\plans\2026-07-19-v184-live-editing.md`

最终发布判断：**CT 0.8.4 的 64 个可枚举直接补丁候选已完成全量审计，安全覆盖为 60/64；离线存档、配装、专精、召唤石、UI 与监测门禁已经落地，正式 EXE 已按最终源码清理构建并记录产物哈希。由于本轮没有连接正在运行的目标游戏，运行时部分仍不得宣称“所有 CT 功能与全场景已实机验证”；其余公式、图标和实验项缺口按上文继续 fail-closed。**
