<p align="center">
  <img src="build/appicon.png" width="112" alt="GBFR PE Patch Tool" />
</p>

<h1 align="center">GBFR PE Patch Tool</h1>

<p align="center">
  面向《碧蓝幻想：Relink》DLC 2.0.2 的 Windows 存档编辑、实时工具与只读公式校准工作台
</p>

<p align="center">
  <a href="https://github.com/Whitelinker574/GBFR-PE-Patch-Tool/releases/latest">下载 v1.91.3</a> ·
  <a href="README_EN.md">English</a> ·
  <a href="docs/README.md">文档与证据</a>
</p>

<p align="center">
  <a href="https://github.com/Whitelinker574/GBFR-PE-Patch-Tool/actions/workflows/ci.yml"><img src="https://github.com/Whitelinker574/GBFR-PE-Patch-Tool/actions/workflows/ci.yml/badge.svg" alt="CI" /></a>
</p>

![功能主页](docs/screenshots/home.png)

这个项目把原本分散的存档修改、运行时功能、配装构造和公式取证整理为同一个桌面应用。界面明确区分三种工作方式：退出游戏后修改存档、连接游戏进行实时修改、只读监测运行时数据。它不是官方工具，也不会替游戏解锁尚未开放的 DLC 系统。

## 快速开始

1. 从 [Releases](https://github.com/Whitelinker574/GBFR-PE-Patch-Tool/releases/latest) 下载 Windows amd64 压缩包，并核对随包提供的 SHA-256。
2. 修改存档前完全退出游戏；修改 EXE 或使用实时功能前先备份目标文件。
3. 打开程序后按用途选择左侧入口：`存档修改（离线）`、`内存注入（实时）` 或 `内存监测（只读）`。
4. 每次写入前确认存档位、角色、物品和槽位；写入后使用页面回读结果确认目标值。

默认存档目录：

```text
C:\Users\<用户名>\AppData\Local\GBFR\Saved\SaveGames\
```

程序会同时显示识别出的多个存档位，也可以手动浏览文件。离线写入使用备份、校验和修复、临时文件、原子替换和重新读取；不要在游戏运行时同时修改同一份存档。

## 功能总览

| 工作区 | 页面 | 主要功能 |
| --- | --- | --- |
| 存档修改（离线） | 7 | 配装预设、因子、物品与武器、祝福、召唤石存档、角色使用次数、任务与称号记录 |
| 内存注入（实时） | 10 | 综合实时修改、因子/祝福石即时编辑、配装录制与复刻、召唤石、角色上限突破、战斗/角色/任务补丁、怪物实验 |
| 内存监测（只读） | 2 | 队伍与选中物品监测、角色最终面板与 A/B/A/B 公式采样 |
| 工具与设置 | 3 | 版本适配、语言与显示、游戏文件维护 |

### 存档与配装

![配装预设与三存档入口](docs/screenshots/loadout-presets.png)

- 因子、祝福和召唤石在存档、内存与配装构造入口共用同一套 2.0.2 目录。
- 配装工作台可读取每个角色的 15 个预设槽，并编辑武器、12 个因子、4 个技能、三方向专精与召唤石。
- 单套配装可以导入或导出；导入只匹配当前存档已经拥有的资源，不做存档批量搬运。
- 天然组合、常见等级与召唤石合法性只作提示。只要值可以编码，用户选择默认允许写入；目标所有权、容量、整数范围、校验和与写后回读仍是硬性检查。
- 未进入 DLC 的存档可能存在预分配记录，但写入记录不等于解锁系统。游戏是否接受和展示该记录仍由游戏自身状态决定。

配装估算会把存档可证明的角色成长、武器、因子、专精、上限突破和召唤石来源分项展示；能够从游戏读取最终 HP、攻击力、暴击率和昏厥值时，会与草稿并排对照。没有现场证据的结果继续标为估算、候选或未闭环，不冒充真实最终公式。

### 实时编辑与补丁

![战斗规则补丁](docs/screenshots/patch-combat.png)

- 所有实时写入绑定当前进程、连接所有权令牌和选中目标，切换角色、重载存档或目标失效时拒绝沿用旧快照。
- 战斗规则、角色机制、任务便利三个补丁页共用一条常驻连接；切换页面不会自动恢复已开启功能。只有明确“恢复全部并断开”或退出程序时才统一清理。
- 补丁目录锁定 DLC 2.0.2 的原字节、特征码和写后回读。冲突功能由目录显式管理，不能为通过测试而跳过版本守卫。
- 自动完美格挡的连招中断修复、单人队伍监测和训练场防御样本已经完成现场重复验证；未逐项实机覆盖的运行时功能不会被笼统标成全场景通过。

实时功能仅用于本地单人环境。重启游戏后需要重新连接；不要把运行时修改带入联机房间或影响其他玩家。

### 只读监测与公式采样

![角色公式采样](docs/screenshots/formula-sampler.png)

公式采样器只申请进程查询与读取权限，不安装 Hook、不注入、不写内存，也不修改存档。它可以连续读取最终 HP、攻击力、暴击率和昏厥值，记录一次稳定前值、过渡期和稳定后值，并保留严格 A1/B1/A2/B2 模式用于可重复验证。导出的证据会删除 PID、模块基址、绝对地址、用户名和本地路径。

防御、减伤、伤害上限和条件技能不一定改变人物面板，因此需要训练场受击或木桩样本。四项面板没有变化只算负观察，不能据此断言效果不存在。

### 游戏文件维护

游戏文件维护页用于识别目标 EXE、显示版本边界、创建 `.bak`、应用已审核补丁和恢复备份。遇到未知 EXE、特征不唯一、原字节不符或地址越界时会停止，而不是尝试写入相似位置。Steam 文件验证也可用于恢复原始文件。

## 写入与证据边界

| 层级 | 工具会做什么 | 不会承诺什么 |
| --- | --- | --- |
| 存档写入 | 备份、事务写入、校验和、原子替换、回读 | 不代替游戏解锁 DLC 系统，不保证任意非法组合被游戏消费 |
| 运行时写入 | 版本守卫、所有权令牌、目标快照、写后回读、恢复原值 | 不支持联机使用，不把未知版本当兼容版本 |
| 只读监测 | 查询进程并读取有限对象、稳定性验证、脱敏导出 | 不扫描或导出整个进程，不把候选字段当最终公式 |
| 配装估算 | 展示来源、证据等级和与实读差值 | 不用固定截图值或未经验证的缩放硬凑一致 |

详细边界见 [公式与证据等级](docs/FORMULAS_2.0.2.md)、[公式采样操作说明](docs/角色公式采样操作说明.md) 和 [存档/内存目录一致性](docs/evidence/save-memory-table-parity.md)。

## 项目结构

```text
.
├─ app.go / *_store.go / *_gen.go    Wails API、存档解析、目录与事务写入
├─ readonly_game_process.go          只读进程连接与生命周期
├─ runtime_*.go / runtime_patch_*.go  运行时定位、补丁会话、守卫和回读
├─ formula_*.go                      面板定位、采样状态机与脱敏证据包
├─ data/                              嵌入的 2.0.2 目录、布局和证据数据
├─ frontend/
│  ├─ src/components/                Vue 页面与共享组件
│  ├─ src/*.test.js                  前端目录、交互和安全回归测试
│  └─ wailsjs/                       Wails 自动生成绑定
├─ resources/                         随程序嵌入的原生资源
├─ src_dll/                           可复现的 patch_core 原生组件源码
├─ tools/                             数据审计、目录生成与图标同步脚本
├─ docs/                              维护文档、公开截图与脱敏证据
├─ build/                             Wails 元数据、图标与本地构建输出
└─ .github/workflows/ci.yml           Go、前端和静态检查
```

线上 `tools/` 只保留重建当前数据或发布所需的可复现脚本；一次性现场 QA、截图脚本、API 凭据、交接包和机器专用文件不进入仓库。自动化单元测试属于发布验证的一部分，会继续保留。

## 本地构建与验证

环境：Windows amd64、Go 1.25+、Node.js/npm、Wails CLI v2.13、WebView2 Runtime。只有重建 `src_dll/patch_core` 时需要 Visual Studio/MSBuild。

```powershell
cd frontend
npm ci
npm run build
cd ..

go test ./...
go vet ./...
node --test frontend/src/*.test.js
wails build -platform windows/amd64 -clean
```

产物位于 `build\bin\GBFR PE Patch Tool.exe`。发布前还应启动成品程序，核对版本号、主要页面、更新入口和 SHA-256。

## 反馈问题

提交问题时请说明工具版本、游戏版本、使用页面、目标存档位和可复现步骤。运行时问题请附脱敏后的页面状态或证据包；不要上传真实存档、PID、绝对地址、用户名或包含个人路径的截图。

## 使用边界与参考说明

本项目仅供学习和个人本地使用，与 Cygames、SEGA、游戏发行方以及下列社区作者均无隶属、合作、授权或商业关系。修改存档、游戏文件或运行时内存存在损坏数据、失去进度或触发游戏自身校验的风险；请只处理自己有权使用的文件，保留可恢复备份，并自行承担使用结果。禁止将本项目包装为付费代改服务，也不要在联机环境中影响其他玩家。

仓库不包含、镜像、破解或转售任何第三方付费表、会员内容或受限下载。运行时补丁目录只以 DLC 2.0.2 游戏 EXE 的哈希、特征码、原字节、唯一命中、写后回读和现场样本作为发布证据；社区公开演示仅用于功能对照与测试思路参考，不是本仓库的运行依赖或下载来源。

为便于复核，下面只列出曾用于交叉检查的公开资料，不代表合作、授权、代码移植或责任背书：存档和因子记录的早期资料可见 [BitterG 的公开项目](https://github.com/BitterG) 与 [公开页面](https://b23.tv/uRLYpW8)；配装交互可参考 [意地悪い骷髅](https://b23.tv/xhiZ7fm) 的 [配装模拟器](https://lib.kannanote.top/%e7%a2%a7%e8%93%9d%e9%85%8d%e8%a3%85%e6%a8%a1%e6%8b%9f%e5%99%a8/)；中文术语对照可见 [LKong621](https://b23.tv/mnwxgDf) 的公开内容；数据提取使用过 [Nenkai](https://github.com/Nenkai) 的公开工具；召唤石提示还与 [SinnohDawn](https://b23.tv/lKSX4zy) 的公开说明及 [Relink Summon](https://relinksummon.fate-go.top) 做过对照。其他公开演示只用于核对功能名称和测试场景，仓库不提供相关付费内容、下载入口或复刻资料。
